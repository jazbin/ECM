# Assumptions Register

| Date (UTC) | Assumption | Rationale |
| --- | --- | --- |
| 2026-02-24 | Logging files live under artifacts/logs and STATUS.md is in repo root. | No existing runbook or status convention found; this is the minimal, reversible layout. |
| 2026-02-25 | OpenFOAM v2506 environment is available at `/opt/openfoam` and can be used for builds/runs. | WM_PROJECT_DIR was set and used for `Allwmake` and case verification. |
| 2026-02-25 | Project license set to GPLv3 to align with OpenFOAM licensing expectations. | OpenFOAM contributions are GPLv3; repo extends OpenFOAM code. |
| 2026-02-25 | CI workflow uses `openfoam/openfoam:v2506` container image for builds/tests. | Matches repo's OpenFOAM version; adjust if a different container is preferred. |
| 2026-03-12 | OpenFOAM 2D workflows use single-cell-thick 3D meshes with `empty` front/back patches rather than true 2D meshes. | OpenFOAM operates on 3D poly meshes; 2D cases are represented as 1-cell-thick meshes with `empty` patches. |
| 2026-03-17 | Consultant CLI wrapper can write per-run CSV with columns including `Q_GEN_W`, `q_ah_next`, `V_RC_1`, `V_RC_2`, `H`, and accepts `--write-csv` plus `--csv-prefix`. | Required to extract total heat and state updates for lumped coupling until a file-based I/O contract exists. |
| 2026-03-17 | Python mock backend requires `numpy` and `pandas` when using `ecm/ecm_coupler.py` with `mock_ecm_backend.py`. | The mock ECM backend loads optional CSV tables and uses NumPy/Pandas for calculations. |
| 2026-03-18 | Khan OSF dataset CSVs provide `time/s` and `Ecell/V` (no temperature), and Stanford 2021 XLSX files expose `Test_Time(s)`, `Voltage(V)`, and `Surface_Temp(degC)` in `sheet1`. | Needed to implement voltage/temperature validation scripts without external dependencies. |
| 2026-03-18 | `cellFull.stl` coordinates are treated as meters (no scaling) with the main cylinder axis along +Z; concentric radii at ~110 and ~120 define three regions (core, shell, ambient). | STL vertex bounds and radius clustering support this interpretation; used to place snappyHexMesh `locationsInMesh`. |
| 2026-03-21 | Non-lumped ECM/mesh coupling uses a mapping table with per-pair weights representing intersection volumes (or relative weights in synthetic tests). | Required to aggregate mesh temperatures to ECM elements and distribute `qVol` back to mesh cells without changing the binary IO contract. |
| 2026-03-21 | Non-lumped cylinder case uses an elevated ambient dynamic viscosity to emulate stronger heat rejection without changing geometry or solver type. | Needed to keep steady-state temperatures in a sensible range for quick ECM/mesh coupling checks. |
| 2026-03-23 | `cases/lumped` uses `chtMultiRegionFoam` (transient) with `frozenFlow yes` for the ambient fluid region to represent a conduction-only placeholder fluid while still satisfying the solver's requirement for a fluid region. | Prevents pseudo-time artifacts from steady SIMPLE when ECM coupling expects real time stepping; avoids unnecessary pressure/momentum work in the dummy ambient region. |
| 2026-03-24 | `cellFull_2170.stl` is a non-uniformly scaled version of `cellFull_m.stl` to approximate a 2170 cell envelope (radius ~10.5 mm, height ~70 mm), and snappyHexMesh locationsInMesh were scaled accordingly. | The original STL dimensions were ~0.24 m height and ~0.24 m diameter, far larger than 2170; scaling aligns the geometry to target cell size without rebuilding the CAD. |
| 2026-03-25 | `cases/lumped_solid` should remain solids-only with no ambient cellZone created by snappyHexMesh locationsInMesh. | Solid-only solver expects three regions (jellyRoll, shell, cap); ambient is removed from mesh generation to avoid extra zones. |
| 2026-03-25 | Solids-only multi-region runs default to explicit temperature coupling when `useImplicit true` is detected; set `allowImplicitSolidsOnly true` in `fvSolution` to permit implicit coupling (experimental) while the fvMatrixAssembly undershoot issue remains unresolved. | With `useImplicit true` in solids-only runs, interface temperatures and local minima dropped below the initial 313.15 K despite positive volumetric heating; explicit coupling avoided the artifact in short runs. |
| 2026-03-25 | Solids-only implicit coupling may apply a minimum temperature clamp via `implicitClampTMin` (in `fvSolution`) to prevent non-physical undershoot while the fvMatrixAssembly root cause remains under investigation. | Short implicit runs showed Tmin dropping below the initial temperature; a configurable clamp keeps Tmin at or above the chosen threshold. |
| 2026-03-27 | In `cases/distributed_solid`, residual q(t) jaggedness after enabling linear interpolation was primarily temporal: `ECM_CALL_EVERY_N_STEPS=3` still produces slope changes every third CFD step, especially with `relaxation=1.0`. | Comparing logs showed wrapper interpolation was active, but the applied source still changed in a 3-step cadence; switching to per-step ECM calls and `relaxation=0.5` removed that pattern in the latest rerun. |
| 2026-03-27 | Headless report generation should use PyVista only for OpenFOAM reading/slicing and matplotlib for image rendering when ParaView/PyVista off-screen OpenGL backends are unavailable. | The environment can read and slice multi-region OpenFOAM data with PyVista, but `pvpython`/VTK screenshot rendering fails under off-screen EGL/OSMesa; matplotlib rendering from the slice geometry avoids that dependency. |
| 2026-03-27 | Distributed runtime attribution should be benchmarked on matched 30 s cases as solver-only, solver plus mock coupling, and solver plus full ECM, then reported as percentages of the full 30 s run. | The raw solver log does not isolate transport/wrapper overhead from ECM compute; matched benchmark runs provide a defensible split. |
| 2026-03-27 | The lumped quick fixed-ambient validation is treated as a reduced-order sanity check by fitting a first-order `R_th C_th` model to the simulated capacity-weighted temperature response, not as an external experimental validation. | It is useful for rapid solver-behavior screening, but the fitted `R_th` and `tau` come from the simulation itself and therefore do not independently validate the physical model. |
| 2026-03-27 | The fixed-ambient half-power vs full-power comparison is a valid linearity sanity check for the current lumped thermal setup because material properties are constant and the governing conduction problem is linear in the imposed volumetric source. | Under those assumptions, halving the source should halve the temperature rise everywhere over the transient; large deviations would indicate implementation or boundary-condition issues. |
| 2026-03-27 | Region-level `h` equation relaxation in the per-region `system/<region>/fvSolution` files is part of the physical behavior of these validation runs and must be checked explicitly when judging energy-balance accuracy. | The lumped adiabatic benchmark used `relaxationFactors { equations { h 0.3; } }`, which reduced the one-region adiabatic enthalpy rise by the same factor and invalidated that benchmark until it was set to `1.0`. |
| 2026-03-27 | The lumped multi-region adiabatic benchmark is only valid if the outer `shell` and `cap` boundaries are explicitly reset to `zeroGradient` after cloning the base case. | The base/working copies can retain `externalWall fixedValue 313.15`, which turns the intended adiabatic benchmark into a heat-rejection benchmark and produces a large apparent energy deficit. |
| 2026-03-27 | The default development gate should stay limited to time-step independence, energy balance, and mesh independence, with broader checks reserved for the extended pre-release suite. | These three checks gave the best signal-to-cost ratio for repeated iteration on the lumped solver/ECM path while the rest remain useful but secondary. |
| 2026-03-27 | The lumped/solid development validation cases do not need the same conservative diffusion cap used during earlier exploratory runs; a looser `maxDi` is acceptable for fast development-time control tests if the results are checked against the same invariants. | With `maxDi=50`, pure solid validation controls were driven to unnecessarily small timesteps by the shell region, making runtime dominated by timestep control rather than by the validation question itself. |
| 2026-03-27 | For lumped forward validation, `maxDi=100` is an acceptable development default; `maxDi=500` is too loose for analytic energy-balance controls. | Quick calibration and forward campaign runs showed `maxDi=100` retained acceptable adiabatic energy error (~`-1.89%`) while reducing runtime significantly, whereas `maxDi=500` produced large energy error (~`-8.6%`). |
| 2026-03-27 | Distributed forward-validation case clones must delete inherited runtime snapshots, especially `artifacts/runtime/ecm_last_good.bin`, before execution. | The apparent distributed startup transient in the first forward campaign was traced to cloned last-good ECM snapshots being loaded at startup and then blended into new runs by `temporalInterpolation linear` plus source relaxation. After removing cloned runtime snapshots, the distributed zero-current run starts with `Q_sum_check = 0` from the first step. |
| 2026-03-27 | Validation/benchmark solver launches should use the direct login-shell invocation path and must not re-source/override the OpenFOAM environment inside the shared `_run()` helper. | The distributed shared-state case matched lumped exactly when launched directly from the shell, but diverged materially when launched through the old `_run()` wrapper that re-sourced `/opt/openfoam/etc/bashrc` and overrode `FOAM_USER_*` paths. Switching `_run()` to the direct login-shell invocation removed the false fixed-current discrepancy and restored lumped-distributed agreement to numerical tolerance. |
| 2026-03-27 | With `h` equation relaxation set to `1.0` and true adiabatic outer walls, the lumped multi-region fixed-power benchmark agrees with the analytic adiabatic temperature rise to within about `0.4 K` at `60 s`, and the standard OpenFOAM temperature-coupled patch matches the custom ECM patch result. | The corrected reruns of `validation_lumped_adiabatic_power` and `validation_lumped_adiabatic_stdpatch` both produced the same region-average temperatures and a capacity-weighted average within about `0.4 K` of `T0 + Pt/C`. |
| 2026-03-27 | Portable case handoff should ship mesh-generation inputs and let `Allrun` call `Allmesh` automatically if split meshes are absent. | This keeps the export small and workstation-agnostic while preserving autonomous execution for `cases/lumped_solid` and `cases/distributed_solid`. |
| 2026-03-27 | Portable launch scripts should prefer package-local `.openfoam/bin` and `.openfoam/lib` relative to the package root, not a hard-coded workspace path. | The package is meant to be relocatable between workstations, and switching lumped/distributed modes should remain a config change rather than a shell-environment change. |
| 2026-03-28 | The distributed coupling code supports many-to-many weighted mapping, but the earlier active `mapping_table_axial6_radial3_2170mesh.csv` behaved as a hard assignment table; true overlap weighting now requires the generated `..._overlap.csv` file. | Inspection of the active file showed one row per `meshKey` and uniform stored weights, whereas the new overlap-generated file has duplicated `meshKey` rows and nontrivial split weights across section boundaries. |
| 2026-03-28 | The current overlap-weighted generator approximates true cell-to-section overlap by direct full-cell assignment away from boundaries and structured subcell sampling only for CFD cells that straddle axial or radial section cuts. | This provides a practical many-to-many volume-weighted map from existing mesh geometry without needing an exact polyhedral intersection library. |
| 2026-03-30 | The client PDF header image must remain fixed in size and position; overlap fixes should be handled only by moving page content lower on the cover and contents pages. | The user explicitly requested keeping the image untouched and only resolving the top-of-page text collisions. |
| 2026-03-30 | On body pages, the client PDF header must explicitly restore the cursor to the safe post-banner top margin after drawing the label row. | Without a final `set_y()` in `header()`, some pages can start content inside the header band even when the nominal top margin is correct. |
| 2026-03-30 | The residual-risk table should wrap the left `Risk area` column and size each row from the taller of the left/right cells, while leaving the shared table renderer unchanged for simpler tables elsewhere in the report. | The user requested a targeted fix for the bleeding text in that specific table rather than a global table-layout change. |
| 2026-03-30 | The client report should use the `30 s` benchmark as the basis for the quoted `4,057%` JSON-spawn overhead, use `under 1.5 s` consistently for the 5,000-cell ceiling, describe the persistent-process path as the current validated backend-state model, and phrase backend swap claims as expected rather than proven. | The user identified internal inconsistencies and over-strong wording that would otherwise weaken confidence in the document. |
| 2026-03-30 | Figure 8.7 should be regenerated from the overlap case with the same millimeter-axis / sparse-tick presentation used by the cleaner distributed 300 s cross-section figure, rather than reusing the older meter-axis asset. | The original `SF14_extended_300s_crosssection.png` had overlapping x-axis labels, while the regenerated mm-axis style matched the acceptable Figure 8.4 presentation. |
