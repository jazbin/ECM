FoamFile
{
    version     2.0;
    format      ascii;
    class       dictionary;
    object      probeT6;
}

type            probes;
libs            ( sampling );

writeControl    timeStep;
writeInterval   1;

fields          ( T );

probeLocations
(
    ( 0 0 0.070019 )   // T6 — cap top, sampled just inside the cap cell
);
