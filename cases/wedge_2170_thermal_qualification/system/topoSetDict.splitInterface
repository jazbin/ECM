/*--------------------------------*- C++ -*----------------------------------*\
| =========                 |                                                 |
| \\      /  F ield         | OpenFOAM: The Open Source CFD Toolbox           |
|  \\    /   O peration     | Version:  2506                                  |
\*---------------------------------------------------------------------------*/
FoamFile
{
    version     2.0;
    format      ascii;
    class       dictionary;
    object      topoSetDict;
}
// * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * //

actions
(
    // Bottom contact: from Salome-defined face zone
    {
        name    bottomContactFaces;
        type    faceSet;
        action  new;
        source  zoneToFace;
        zone    interface_rotated;
    }

    // Radial contact: remaining interface faces
    {
        name    radialContactFaces;
        type    faceSet;
        action  new;
        source  patchToFace;
        patch   jellyRoll_rotated_to_shell_rotated;
    }
    {
        name    radialContactFaces;
        type    faceSet;
        action  subtract;
        source  faceSet;
        set     bottomContactFaces;
    }
);

// ************************************************************************* //
