FoamFile
{
    version     2.0;
    format      ascii;
    class       dictionary;
    object      probeT1;
}

type            probes;
libs            ( sampling );

writeControl    timeStep;
writeInterval   1;

fields          ( T );

probeLocations
(
    ( 0 0 1e-06 )   // T1 — shell base, sampled just inside the shell cell
);
